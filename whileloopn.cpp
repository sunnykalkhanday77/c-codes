#include<iostream>

using namespace std;

int main (){
    int n=30;
    int count=1;

    while  (count<=n){
   cout << count<<"*\n";
   count ++;  // if we will remove this count variable from program then thus will decklare as a infinite loop 

    }
    cout<<endl;
    return 0;
}